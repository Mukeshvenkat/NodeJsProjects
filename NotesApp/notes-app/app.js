// const fs = require('fs');
// fs.writeFileSync('notes.txt', 'First file writing application!');
// fs.appendFileSync('notes.txt', '\n Data appended to the file');

const getNotes = require('./notes.js');

console.log(getNotes());